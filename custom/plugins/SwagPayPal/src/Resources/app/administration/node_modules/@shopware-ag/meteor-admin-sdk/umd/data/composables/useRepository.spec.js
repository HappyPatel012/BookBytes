var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "./useRepository", "./getRepository", "vue"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    const useRepository_1 = require("./useRepository");
    const getRepository_1 = require("./getRepository");
    const vue_1 = require("vue");
    // Mock dependencies
    jest.mock('./getRepository');
    jest.mock('vue', () => {
        const originalVue = jest.requireActual('vue');
        return Object.assign(Object.assign({}, originalVue), { inject: jest.fn() });
    });
    describe('useRepository', () => {
        const mockProductRepository = { id: 'product-repo' };
        const mockCategoryRepository = { id: 'category-repo' };
        const mockCustomRepository = { id: 'custom-repo' };
        const mockComputedValue = { value: mockProductRepository };
        beforeEach(() => {
            jest.clearAllMocks();
            getRepository_1.getRepository.mockImplementation((entityName, factory) => {
                if (factory)
                    return mockCustomRepository;
                return entityName === 'product' ? mockProductRepository : mockCategoryRepository;
            });
        });
        it('should return a computed ref', () => {
            const repository = (0, useRepository_1.useRepository)('product');
            expect(repository.value).toBe(mockComputedValue.value);
        });
        it('should call getRepository with unwrapped values inside computed', () => __awaiter(void 0, void 0, void 0, function* () {
            const repository = (0, useRepository_1.useRepository)('product');
            expect(repository.value).toBeDefined();
            expect(getRepository_1.getRepository).toHaveBeenCalledWith('product', undefined);
        }));
        it('should handle reactive entity name', () => {
            const entityName = (0, vue_1.ref)('product');
            const repository = (0, useRepository_1.useRepository)(entityName);
            expect(repository.value).toBeDefined();
            expect(getRepository_1.getRepository).toHaveBeenCalledWith('product', undefined);
        });
        it('should handle reactive repository factory', () => {
            const mockFactory = jest.fn();
            const factoryRef = (0, vue_1.ref)(mockFactory);
            const repository = (0, useRepository_1.useRepository)('product', factoryRef);
            expect(repository.value).toBeDefined();
            expect(getRepository_1.getRepository).toHaveBeenCalledWith('product', mockFactory);
        });
        it('should handle both reactive entity name and factory', () => {
            const entityName = (0, vue_1.ref)('product');
            const mockFactory = jest.fn();
            const factoryRef = (0, vue_1.ref)(mockFactory);
            const repository = (0, useRepository_1.useRepository)(entityName, factoryRef);
            expect(repository.value).toBeDefined();
            expect(getRepository_1.getRepository).toHaveBeenCalledWith('product', mockFactory);
        });
    });
});
//# sourceMappingURL=useRepository.spec.js.map