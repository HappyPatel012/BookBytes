var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "semver/functions/cmp", "semver/functions/valid"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    const cmp_1 = __importDefault(require("semver/functions/cmp"));
    const valid_1 = __importDefault(require("semver/functions/valid"));
    function getCompareIsShopwareVersion(getShopwareVersion) {
        return (comparator, versionToCompare) => __awaiter(this, void 0, void 0, function* () {
            const shopwareVersion = yield getShopwareVersion();
            const shopwareSemverVersion = convertToSemver(shopwareVersion);
            const versionSemverToCompare = convertToSemver(versionToCompare);
            return (0, cmp_1.default)(shopwareSemverVersion, comparator, versionSemverToCompare);
        });
    }
    exports.default = getCompareIsShopwareVersion;
    function convertToSemver(version) {
        // Test if version is a 4 digit shopware version with leading 6.
        if (!(0, valid_1.default)(version) && /6\.[\d]+\.[\d]+\.[\d]+/.test(version)) {
            return version.substring(2);
        }
        return version;
    }
});
//# sourceMappingURL=compare-version.js.map