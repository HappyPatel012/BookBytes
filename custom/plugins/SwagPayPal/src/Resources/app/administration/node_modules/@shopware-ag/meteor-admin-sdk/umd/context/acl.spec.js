var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "./acl"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    const acl_1 = __importDefault(require("./acl"));
    describe('createACLHelper', () => {
        it('returns true if the crud privilege is granted', () => __awaiter(void 0, void 0, void 0, function* () {
            const mockGetAppInformation = jest.fn().mockResolvedValue({
                privileges: {
                    read: ['product'],
                },
            });
            const can = (0, acl_1.default)(mockGetAppInformation);
            const result = yield can('product:read');
            expect(result).toBe(true);
        }));
        it('returns false if the crud privilege is not granted', () => __awaiter(void 0, void 0, void 0, function* () {
            const mockGetAppInformation = jest.fn().mockResolvedValue({
                privileges: {
                    read: ['product'],
                },
            });
            const can = (0, acl_1.default)(mockGetAppInformation);
            const result = yield can('order:read');
            expect(result).toBe(false);
        }));
        it('returns true if the additional privilege is granted', () => __awaiter(void 0, void 0, void 0, function* () {
            const mockGetAppInformation = jest.fn().mockResolvedValue({
                privileges: {
                    additional: ['api_service_toggle'],
                },
            });
            const can = (0, acl_1.default)(mockGetAppInformation);
            const result = yield can('api_service_toggle');
            expect(result).toBe(true);
        }));
        it('returns false if the additional privilege is not granted', () => __awaiter(void 0, void 0, void 0, function* () {
            const mockGetAppInformation = jest.fn().mockResolvedValue({
                privileges: {
                    additional: ['system.plugin_maintain']
                },
            });
            const can = (0, acl_1.default)(mockGetAppInformation);
            const result = yield can('order:read');
            expect(result).toBe(false);
        }));
    });
});
//# sourceMappingURL=acl.spec.js.map