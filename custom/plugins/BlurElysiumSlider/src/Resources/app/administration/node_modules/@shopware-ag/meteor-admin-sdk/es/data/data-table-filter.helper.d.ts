import Criteria from './Criteria';
interface Filter {
    id: string;
    label: string;
    type: {
        id: string;
        options: Option[];
    };
}
export interface Option {
    id: string;
    label: string;
}
export declare function addDataTableFilters(criteria: Criteria, filters: Filter[]): Criteria;
export {};
