var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "../repository-adapter", "vue"], factory);
    }
})(function (require, exports) {
    "use strict";
    var __syncRequire = typeof module === "object" && typeof module.exports === "object";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getRepository = void 0;
    const repository_adapter_1 = require("../repository-adapter");
    const vue_1 = require("vue");
    function getRepository(entityName, propRepositoryFactory) {
        if (propRepositoryFactory) {
            return (0, repository_adapter_1.createRepositoryAdapter)(propRepositoryFactory(entityName));
        }
        const injectedFactory = (0, vue_1.inject)('repositoryFactory');
        if (injectedFactory) {
            return (0, repository_adapter_1.createRepositoryAdapter)(injectedFactory.create(entityName));
        }
        /*
         * The use of a Proxy delays the import of the repository until the first method is called.
         * This avoids side effects from the repository import and prevents returning a Promise
         * from this composable, which would force components using it to be async.
         */
        let lazyDataRepository;
        return new Proxy({}, {
            get(_, property) {
                return function (...args) {
                    return __awaiter(this, void 0, void 0, function* () {
                        if (!lazyDataRepository) {
                            lazyDataRepository = (yield __syncRequire ? Promise.resolve().then(() => __importStar(require('../repository'))) : new Promise((resolve_1, reject_1) => { require(['../repository'], resolve_1, reject_1); }).then(__importStar)).default(entityName);
                        }
                        const method = lazyDataRepository[property];
                        return method(...args);
                    });
                };
            },
        });
    }
    exports.getRepository = getRepository;
});
//# sourceMappingURL=getRepository.js.map