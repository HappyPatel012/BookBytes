import { Extension } from '@tiptap/core';
export interface HtmlParseDiffResult {
    hasDiff: boolean;
    originalBeautified: string;
    parsedBeautified: string;
    parsedRaw: string;
}
export declare function formatHtmlForDiff(input: string): Promise<string>;
export declare function getHtmlParseDiff(html: string, extensions: Extension[], opts?: {
    parseFromBeautified?: boolean;
}): Promise<HtmlParseDiffResult>;
